{
  "name": "worker-gratis",
  "version": "1.0.0",
  "description": "Scraper SEACE en Render",
  "main": "index.js",
  "scripts": {
    "build": "npm install && node -e \"try { require('puppeteer-chromium-resolver')().then(res => console.log('Chromium instalado en:', res.executablePath)); } catch(e) { console.error(e); process.exit(1); }\"",
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer": "^22.12.1"
  }
}
